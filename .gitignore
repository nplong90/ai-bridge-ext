node_modules/
*.log
.DS_Store

# Sensitive: live session tokens / captured traffic — never commit
cookies.json
*cook*.json
*.har
.playwright-mcp/

# Extension signing private key (pins the ID) — keep safe, never commit
*.pem
scratch_key.txt

# Local agent / editor config (machine-specific; may leak paths/usernames)
.claude/

# Screenshots / scratch
*.png

# Generated by host/install.ps1 (machine-specific path; may contain an API key)
host/com.aibridge.host.json
host/aibridge-host.bat
